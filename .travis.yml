language: generic
dist: xenial
sudo: required
env:
  matrix:
    - TARGET=xenial_arm64_n
    - TARGET=xenial_armhf_n
    - TARGET=bionic_arm64_n
    - TARGET=bionic_armhf_n
addons:
  apt:
    update: true
    packages:
    - qemu-user-static
    - binfmt-support
services:
  - docker
install:
  # To use the qemu-arm-static in the docker container, host kernel should
  # enable the foreign architecture.
  - sudo apt update
  - sudo apt install -y qemu-user-static binfmt-support
  - update-binfmts --display
  - docker system info

script:
  - ./build.sh $TARGET
before_deploy:
  - docker images
  - docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD
deploy:
  provider: script
  script: docker push nugulinux/buildenv:$TARGET
  on:
    all_branches: true
